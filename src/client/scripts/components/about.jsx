"use strict";

var React = require("react");

var About = React.createClass({
    render: function() {
        return (
            <section>
                <p>The Device Survey is a demo website built using React, Less, Webpack and Node.js</p>
            </section>
        );
    }
});

module.exports = About;
